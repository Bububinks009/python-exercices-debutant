# Calculer l’aire d’un rectangle à partir de la longueur et la largeur saisies.

L = float(input ( " L = "))     # entrer la valeur de L qui ne prend que des réels
l = float(input ( " l = "))     # entrer la valeur de l qui ne prend que des réels
A = L*l                         # calcul de A=L*l
print( " A = " , A )            # affichage du résultat sous forme A=.....
